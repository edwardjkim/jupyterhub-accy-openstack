---
- name: install nfs pakcages
  yum: name=nfs-utils,libnfsidmap state=latest

- name: unmount home
  shell: umount /home || true

- name: mount home
  mount: name=/home src={{ hostvars['nfs_server']['private_ip'] }}:/home fstype=nfs opts={{ nfsoptions }} state=mounted
